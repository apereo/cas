apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cas-server.fullname" . }}-test-connection"
  labels:
    {{- include "cas-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cas-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
