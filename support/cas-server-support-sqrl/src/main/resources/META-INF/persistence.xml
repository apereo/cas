<?xml version="1.0" encoding="UTF-8"?>
<persistence version="1.0" xmlns="http://java.sun.com/xml/ns/persistence">
    <persistence-unit name="javasqrl-persistence" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <class>com.github.dbadia.sqrl.server.data.SqrlIdentity</class>
        <class>com.github.dbadia.sqrl.server.data.SqrlCorrelator</class>
        <class>com.github.dbadia.sqrl.server.data.SqrlUsedNutToken</class>
        <properties>
            <property name="javax.persistence.jdbc.driver" value="${driver}"/>
            <property name="javax.persistence.jdbc.url" value="${url}"/>
            <property name="javax.persistence.jdbc.user" value="${user}"/>
            <property name="javax.persistence.jdbc.password" value="${password}"/>
            
            <property name="hibernate.dialect" value="${dialect}"/>
            <property name="hibernate.hbm2ddl.auto" value="${ddl}"/>
            <property name="hibernate.connection.pool_size" value="${pool}"/>

            <property name="hibernate.jdbc.batch_size" value="${batch}"/>
            <property name="hibernate.default_schema" value="${schema}"/>
            <property name="hibernate.default_catalog" value="${catalog}"/>

            <property name="hibernate.show_sql" value="${showSql}"/>
            <property name="hibernate.format_sql" value="true"/>
            
        </properties>
    </persistence-unit>
</persistence>
