apply plugin: "org.apereo.cas.project-conventions"

description = "Adds shared Memcached support for CAS caching and storage features."
ext {
    publishMetadata = true
    projectMetadata = [
            category: "Memcached Support",
            title: "Memcached via SPY"
    ]
}
dependencies {
    api libraries.memcached
}
