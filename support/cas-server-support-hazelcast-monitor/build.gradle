description = "Apereo CAS Hazelcast Monitoring Support"
dependencies {
    implementation libraries.hazelcast
    
    compile project(":core:cas-server-core-monitor")
    compile project(":support:cas-server-support-hazelcast-ticket-registry")
}
