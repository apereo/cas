description = "Apereo CAS Hazelcast Monitoring Support"
dependencies {
    implementation libraries.hazelcast
    
    api project(":core:cas-server-core-monitor")
    api project(":support:cas-server-support-hazelcast-ticket-registry")
}
