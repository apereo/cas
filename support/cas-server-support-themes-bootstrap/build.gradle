apply plugin: "org.apereo.cas.project-conventions"

description = "Adds Bootstrap-based themes for customizing the CAS user interface."
ext {
    publishMetadata = true
    projectMetadata = [
            title: "Bootstrap Themes",
            category: "User Interface"
    ]
}
dependencies {
    runtimeOnly libraries.webjarsbootstrap
}
