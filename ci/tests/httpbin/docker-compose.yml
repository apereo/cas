version: '3'

services:
  httpbin:
    image: kennethreitz/httpbin
    ports:
      - "9889:80"
  proxy:
    image: fsouza/docker-ssl-proxy
    environment:
      DOMAIN: localhost
      TARGET_HOST: httpbin
    links:
      - httpbin
    ports:
      - "9859:443"
