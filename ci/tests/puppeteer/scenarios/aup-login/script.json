{
  "dependencies": "aup-webflow",
  "properties": [
    "--cas.audit.engine.enabled=false",
    "--cas.server.name=https://localhost:8443",
    "--cas.server.prefix=${cas.server.name}/cas",

    "--cas.service-registry.core.init-from-json=true"
  ]
}
