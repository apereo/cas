{
  "dependencies": "reports",
  "properties": [
    "--cas.server.name=https://localhost:8443",
    "--cas.server.prefix=${cas.server.name}/cas",

    "--cas.monitor.endpoints.endpoint.sso.access=AUTHENTICATED",
    "--cas.monitor.endpoints.form-login-enabled=true",
    "--management.endpoint.sso.enabled=true",
    "--management.endpoints.web.exposure.include=sso"
  ]
}
