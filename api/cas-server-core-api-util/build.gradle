apply plugin: "org.apereo.cas.project-conventions"

description = "Defines shared utility contracts used across CAS modules and extensions."
dependencies {
    implementation libraries.jose4j
    implementation libraries.classgraph
}
