casVersion: 5.1.0
casModuleGroupId: "org.apereo.cas"
casSupportModuleArtifactIdPrefix: "cas-server-support"
gitRemote:

initializr:
  env:
    google-analytics-tracking-code: UA-99132453-1
    boms:
      cas-bom:
        groupId: ${casModuleGroupId}
        artifactId: cas-server-support-bom
        version: "${cas.version}"
    repositories:
      sonatype-releases:
        name: sonatype-releases
        url: http://oss.sonatype.org/content/repositories/releases/
        snapshotsEnabled: false
      sonatype-snapshots:
        name: sonatype-snapshots
        url: http://oss.sonatype.org/content/repositories/snapshots/
        snapshotsEnabled: true
      shibboleth-releases:
        name: shibboleth-releases
        url: https://build.shibboleth.net/nexus/content/repositories/releases
      jasig-dev-legacy:
        name: jasig-dev-legacy
        url: http://developer.jasig.org/repo/content/groups/m2-legacy
      duo-uniconiam:
        name: duo-uniconiam
        url: https://dl.bintray.com/uniconiam/maven
      jitpack:
        name: jitack
        url: https://jitpack.io
  dependencies:
    - name: CAS Acceptable Usage Policy
      content:
        - name: CAS Acceptable Usage Policy
          id: ${casSupportModuleArtifactIdPrefixPrefix}-aup-webflow
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-aup-webflow
          version: ${casVersion}
          bom: cas-bom
          description: Introduce Acceptable Usage Policy into the CAS Authentication Webflow
        - name: CAS Acceptable Usage Policy LDAP
          id: ${casSupportModuleArtifactIdPrefix}-aup-ldap
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-aup-ldap
          version: ${casVersion}
          bom: cas-bom
          description: Acceptable Usage Policy requirements and decisions may be stored inside an LDAP directory.
    - name: CAS Spring Webflow Sessions
      content:
        - name: CAS Hazelcast Session Replication
          id: cas-server-webapp-session-hazelcast
          groupId: ${casModuleGroupId}
          artifactId: cas-server-webapp-session-hazelcast
          version: ${casVersion}
          bom: cas-bom
          description: Enable server-side sessions for Spring Webflow, and replicate sessions via Hazelcast.
        - name: CAS Redis Session Replication
          id: cas-server-webapp-session-redis
          groupId: ${casModuleGroupId}
          artifactId: cas-server-webapp-session-redis
          version: ${casVersion}
          bom: cas-bom
          description: Enable server-side sessions for Spring Webflow, and replicate sessions via Redis.
        - name: CAS MongoDb Session Replication
          id: cas-server-webapp-session-mongo
          groupId: ${casModuleGroupId}
          artifactId: cas-server-webapp-session-mongo
          version: ${casVersion}
          bom: cas-bom
          description: Enable server-side sessions for Spring Webflow, and replicate sessions via MongoDb.
    - name: CAS Service Discovery
      content:
        - name: CAS Eureka Service Discovery Client
          id: ${casSupportModuleArtifactIdPrefix}-eureka-client
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-eureka-client
          version: ${casVersion}
          bom: cas-bom
          description:
    - name: CAS Monitoring
      content:
        - name: CAS Spring Boot Administration Client
          id: ${casSupportModuleArtifactIdPrefix}-bootadmin-client
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-bootadmin-client
          version: ${casVersion}
          bom: cas-bom
          description: Let CAS become a client of Spring Boot Administration for remote monitoring and tracking purposes.
        - name: CAS Memcached Monitor
          id: ${casSupportModuleArtifactIdPrefix}-memcached-monitor
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-memcached-monitor
          version: ${casVersion}
          bom: cas-bom
          description: Monitor the internal state of a Memacached instance.
        - name: CAS EhCache Monitor
          id: ${casSupportModuleArtifactIdPrefix}-ehcache-monitor
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-ehcache-monitor
          version: ${casVersion}
          bom: cas-bom
          description: Monitor the internal state of an EhCache instance.
        - name: CAS Hazelcast Monitor
          id: ${casSupportModuleArtifactIdPrefix}-hazelcast-monitor
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-hazelcast-monitor
          version: ${casVersion}
          bom: cas-bom
          description: Monitor the internal state of a Hazelcast instance.
        - name: CAS JDBC Monitor
          id: ${casSupportModuleArtifactIdPrefix}-jdbc-monitor
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-jdbc-monitor
          version: ${casVersion}
          bom: cas-bom
          description: Monitor the internal state of a JDBC connection.
        - name: CAS LDAP Monitor
          id: ${casSupportModuleArtifactIdPrefix}-ldap-monitor
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-ldap-monitor
          version: ${casVersion}
          bom: cas-bom
          description: Monitor the internal state of an LDAP connection pool.
    - name: CAS OpenID Protocol
      content:
        - name: CAS OpenID Protocol
          id: ${casSupportModuleArtifactIdPrefix}-openid-webflow
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-openid-webflow
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to act as an OpenID identity provider.
    - name: CAS OAuth Protocol
      content:
        - name: CAS OAuth Protocol
          id: ${casSupportModuleArtifactIdPrefix}-oauth-webflow
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-oauth-webflow
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to act as an OAuth2 identity provider.
    - name: CAS OpenID Connect Protocol
      content:
        - name: CAS OpenID Connect Protocol
          id: ${casSupportModuleArtifactIdPrefix}-oidc
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-oidc
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to act as an OpenID Connect identity provider.
    - name: CAS WsFederation Protocol
      content:
        - name: CAS WsFederation Protocol Security Token Service
          id: ${casSupportModuleArtifactIdPrefix}-ws-sts
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-ws-sts
          version: ${casVersion}
          bom: cas-bom
          description: >
            Allow CAS to act as a security token service, used in conjunction with the WsFederation identity provider,
        - name: CAS WsFederation Protocol Identity Provider
          id: ${casSupportModuleArtifactIdPrefix}-ws-idp
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-ws-idp
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to act as a Ws-Fed identity provider.
    - name: CAS REST Protocol
      content:
        - name: CAS REST Protocol
          id: ${casSupportModuleArtifactIdPrefix}-rest
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-rest
          version: ${casVersion}
          bom: cas-bom
          description: Enable the CAS REST API and protocol support.
        - name: CAS Services REST Protocol
          id: ${casSupportModuleArtifactIdPrefix}-rest-services
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-rest-services
          version: ${casVersion}
          bom: cas-bom
          description: Enable the CAS REST Services API to dynamically add services and applications to CAS.
        - name: CAS REST Protocol via X.509
          id: ${casSupportModuleArtifactIdPrefix}-rest-x509
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-rest-x509
          version: ${casVersion}
          bom: cas-bom
          description: Allow the CAS REST API to extract credentials from X.509.
    - name: CAS SAML
      content:
        - name: CAS SAML1.1 Protocol
          id: ${casSupportModuleArtifactIdPrefix}-saml
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-saml
          version: ${casVersion}
          bom: cas-bom
          description: Enable SAML 1.1 protocol support in CAS
        - name: CAS SAML2 Protocol Identity Provider
          id: ${casSupportModuleArtifactIdPrefix}-saml-idp
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-saml-idp
          version: ${casVersion}
          bom: cas-bom
          description: Let CAS act as a SAML2 Identity Provider
        - name: CAS SAML2 MDUI
          id: ${casSupportModuleArtifactIdPrefix}-saml-mdui
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-saml-mdui
          version: ${casVersion}
          bom: cas-bom
          description:
        - name: CAS SAML2 Google Apps Integration
          id: ${casSupportModuleArtifactIdPrefix}-saml-googleapps
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-saml-googleapps
          version: ${casVersion}
          bom: cas-bom
          description:
        - name: CAS SAML2 SP Integrations
          id: ${casSupportModuleArtifactIdPrefix}-saml-sp-integrations
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-saml-sp-integrations
          version: ${casVersion}
          bom: cas-bom
          description:
    - name: CAS SCIM Integration
      content:
        - name: CAS SCIM Integration
          id: ${casSupportModuleArtifactIdPrefix}-scim
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-scim
          version: ${casVersion}
          bom: cas-bom
          description: Turn on JIT provisioning capabilities via SCIM during the authentication flow.
    - name: CAS reCAPTCHA Integration
      content:
        - name: CAS reCAPTCHA Integration
          id: ${casSupportModuleArtifactIdPrefix}-captcha
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-captcha
          version: ${casVersion}
          bom: cas-bom
          description:
    - name: CAS Groovy Shell
      content:
        - name: CAS Groovy Shell
          id: ${casSupportModuleArtifactIdPrefix}-shell
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-shell
          version: ${casVersion}
          bom: cas-bom
          description:
    - name: CAS Audits
      content:
        - name: CAS Audit JDBC
          id: ${casSupportModuleArtifactIdPrefix}-audit-jdbc
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-audit-jdbc
          version: ${casVersion}
          bom: cas-bom
          description: Track CAS authentication audit logs inside a JDBC instance.
    - name: CAS Logging
      content:
        - name: CAS Logging CloudWatch
          id: cas-server-core-logging-config-cloudwatch
          groupId: ${casModuleGroupId}
          artifactId: cas-server-core-logging-config-cloudwatch
          version: ${casVersion}
          bom: cas-bom
          description: Enable CAS logging support for AWS CloudWatch
        - name: CAS Logging Sentry
          id: ${casSupportModuleArtifactIdPrefix}-sentry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-sentry
          version: ${casVersion}
          bom: cas-bom
          description: Enable CAS logging support for Sentry.
    - name: CAS Service Registries
      content:
        - name: CAS DynamoDb Service Registry
          id: ${casSupportModuleArtifactIdPrefix}-dynamodb-service-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-dynamodb-service-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS registered service definitions inside DynamoDb.
        - name: CAS Couchbase Service Registry
          id: ${casSupportModuleArtifactIdPrefix}-couchbase-service-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-couchbase-service-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS registered service definitions inside Couchbase.
        - name: CAS JPA Service Registry
          id: ${casSupportModuleArtifactIdPrefix}-jpa-service-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-jpa-service-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS registered service definitions inside a relational database.
        - name: CAS LDAP Service Registry
          id: ${casSupportModuleArtifactIdPrefix}-ldap-service-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-ldap-service-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS registered service definitions inside an LDAP directory.
        - name: CAS MongoDb Service Registry
          id: ${casSupportModuleArtifactIdPrefix}-mongo-service-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-mongo-service-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS registered service definitions inside MongoDb.
        - name: CAS YAML Service Registry
          id: ${casSupportModuleArtifactIdPrefix}-yaml-service-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-yaml-service-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS registered service definitions as YAML files.
        - name: CAS JSON Service Registry
          id: ${casSupportModuleArtifactIdPrefix}-json-service-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-json-service-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS registered service definitions as JSON files.
    - name: CAS Ticket Registries
      content:
        - name: CAS Hazelcast Ticket Registry
          id: ${casSupportModuleArtifactIdPrefix}-hazelcast-ticket-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-hazelcast-ticket-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS tickets via Hazelcast.
        - name: CAS EhCache Ticket Registry
          id: ${casSupportModuleArtifactIdPrefix}-ehcache-ticket-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-ehcache-ticket-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS tickets via Ehcache.
        - name: CAS Memcached Ticket Registry
          id: ${casSupportModuleArtifactIdPrefix}-memcached-ticket-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-memcached-ticket-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS tickets via Memcached.
        - name: CAS JPA Ticket Registry
          id: ${casSupportModuleArtifactIdPrefix}-jpa-ticket-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-jpa-ticket-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS tickets via a relational database.
        - name: CAS Ignite Ticket Registry
          id: ${casSupportModuleArtifactIdPrefix}-ignite-ticket-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-ignite-ticket-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS tickets via Apache Ignite.
        - name: CAS Couchbase Ticket Registry
          id: ${casSupportModuleArtifactIdPrefix}-couchbase-ticket-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-couchbase-ticket-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS tickets via Couchbase.
        - name: CAS Infinispan Ticket Registry
          id: ${casSupportModuleArtifactIdPrefix}-infinispan-ticket-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-infinispan-ticket-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS tickets via Infinispan.
        - name: CAS Redis Ticket Registry
          id: ${casSupportModuleArtifactIdPrefix}-redis-ticket-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-redis-ticket-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS tickets via Redis.
        - name: CAS MongoDb Ticket Registry
          id: ${casSupportModuleArtifactIdPrefix}-mongo-ticket-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-mongo-ticket-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS tickets via MongoDb.
        - name: CAS DynamoDb Ticket Registry
          id: ${casSupportModuleArtifactIdPrefix}-dynamodb-ticket-registry
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-dynamodb-ticket-registry
          version: ${casVersion}
          bom: cas-bom
          description: Manage and store CAS tickets via DynamoDb.
    - name: CAS SMS Messaging
      content:
        - name: CAS Twillio SMS Messaging
          id: ${casSupportModuleArtifactIdPrefix}-sms-twillio
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-sms-twillio
          version: ${casVersion}
          bom: cas-bom
          description: Integrate with Twillio as an SMS Service Provider.
        - name: CAS TextMagic SMS Messaging
          id: ${casSupportModuleArtifactIdPrefix}-sms-textmagic
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-sms-textmagic
          version: ${casVersion}
          bom: cas-bom
          description: Integrate with TextMagic as an SMS Service Provider.
        - name: CAS Clickatell SMS Messaging
          id: ${casSupportModuleArtifactIdPrefix}-sms-clickatell
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-sms-clickatell
          version: ${casVersion}
          bom: cas-bom
          description: Integrate with Clickatell as an SMS Service Provider.
    - name: CAS Graphical User Authentication
      content:
        - name: CAS Graphical User Authentication
          id: ${casSupportModuleArtifactIdPrefix}-gua
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-gua
          version: ${casVersion}
          bom: cas-bom
          description: >
            Graphical user authentication, sometimes also known as ‘login images’ are a form of login verification (i.e. second factor) where a
            site presents the user with an image previously selected by the user at the time the account is created.
          keywords:
            - gua
    - name: CAS Risk-based Multifactor Authentication
      content:
        - name: CAS Risk-based Multifactor Authentication
          id: ${casSupportModuleArtifactIdPrefix}-electrofence
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-electrofence
          version: ${casVersion}
          bom: cas-bom
          description: Let CAS calculate a risk score for authentication requests and block suspicious activity.
    - name: CAS Multifactor Authentication Trusted Device/Browser
      content:
        - name: CAS Multifactor Authentication Trusted Device/Browser Core
          id: ${casSupportModuleArtifactIdPrefix}-trusted-mfa
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-trusted-mfa
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to remember/trust devices/browsers in multifactor authentication scenarios.
        - name: CAS Multifactor Authentication Trusted Device/Browser JDBC
          id: ${casSupportModuleArtifactIdPrefix}-trusted-mfa-jdbc
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-trusted-mfa-jdbc
          version: ${casVersion}
          bom: cas-bom
          description: Store trusted devices/browsers in multifactor authentication scenarios in a JDBC instance.
        - name: CAS Multifactor Authentication Trusted Device/Browser MongoDb
          id: ${casSupportModuleArtifactIdPrefix}-trusted-mfa-mongo
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-trusted-mfa-mongo
          version: ${casVersion}
          bom: cas-bom
          description:  Store trusted devices/browsers in multifactor authentication scenarios in a MongoDb instance.
        - name: CAS Multifactor Authentication Trusted Device/Browser REST
          id: ${casSupportModuleArtifactIdPrefix}-trusted-mfa-rest
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-trusted-mfa-rest
          version: ${casVersion}
          bom: cas-bom
          description: Store trusted devices/browsers in multifactor authentication scenarios via a REST API.
    - name: CAS Service Access Strategies
      content:
        - name: CAS Grouper Service Access Strategies
          id: ${casSupportModuleArtifactIdPrefix}-grouper-access
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-grouper
          version: ${casVersion}
          bom: cas-bom
          description: Control access to CAS services via Internet2's Grouper.
    - name: CAS Multifactor Authentication Triggers
      content:
        - name: CAS Grouper Multifactor Authentication Trigger
          id: ${casSupportModuleArtifactIdPrefix}-grouper-mfa
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-grouper
          version: ${casVersion}
          bom: cas-bom
          description: Trigger multifactor authentication events based on Internet2 Grouper
          keywords:
            - mfa
        - name: CAS Shibboleth Multifactor Authentication Trigger
          id: ${casSupportModuleArtifactIdPrefix}-shibboleth
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-shibboleth
          version: ${casVersion}
          bom: cas-bom
          description: Trigger multifactor authentication events for SAML2 service providers delegated to CAS via a Shibboleth IdP.
          keywords:
            - mfa
    - name: CAS Multifactor Authentication
      content:
        - name: CAS Duo Security Multifactor Authentication
          id: ${casSupportModuleArtifactIdPrefix}-duo
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-duo
          version: ${casVersion}
          bom: cas-bom
          description: Enable multifactor authentication via Duo Security.
          keywords:
            - mfa
        - name: CAS YubiKey Multifactor Authentication
          id: ${casSupportModuleArtifactIdPrefix}-yubikey
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-yubikey
          version: ${casVersion}
          bom: cas-bom
          description: Enable multifactor authentication via YubiKey.
          keywords:
            - mfa
        - name: CAS Google Authenticator Multifactor Authentication
          id: ${casSupportModuleArtifactIdPrefix}-gauth
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-gauth
          version: ${casVersion}
          bom: cas-bom
          description: Enable multifactor authentication via Google Authenticator.
          keywords:
            - mfa
            - gauth
        - name: CAS Google Authenticator MongoDb Multifactor Authentication
          id: ${casSupportModuleArtifactIdPrefix}-gauth-mongo
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-gauth-mongo
          version: ${casVersion}
          bom: cas-bom
          description: Store google authenticator device registrations inside a MongoDb instance.
          keywords:
            - mfa
            - gauth
        - name: CAS Google Authenticator JDBC/JPA Multifactor Authentication
          id: ${casSupportModuleArtifactIdPrefix}-gauth-jpa
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-gauth-jpa
          version: ${casVersion}
          bom: cas-bom
          description: Store google authenticator device registrations inside a JDBC instance.
          keywords:
            - mfa
            - gauth
        - name: CAS Authy Multifactor Authentication
          id: ${casSupportModuleArtifactIdPrefix}-authy
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-authy
          version: ${casVersion}
          bom: cas-bom
          description: Enable multifactor authentication via Authy.
          keywords:
            - mfa
        - name: CAS Microsoft Azure Multifactor Authentication
          id: ${casSupportModuleArtifactIdPrefix}-azure
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-azure
          version: ${casVersion}
          bom: cas-bom
          description: Enable multifactor authentication via Microsoft Azure.
          keywords:
            - mfa
        - name: CAS U2F FIDO Multifactor Authentication
          id: ${casSupportModuleArtifactIdPrefix}-u2f
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-u2f
          version: ${casVersion}
          bom: cas-bom
          description: Enable multifactor authentication via FIDO U2F.
          keywords:
            - mfa
    - name: CAS Surrogate Authentication
      content:
        - name: CAS Surrogate Authentication
          id: ${casSupportModuleArtifactIdPrefix}-surrogate-authentication
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-surrogate-authentication
          version: ${casVersion}
          bom: cas-bom
          description: Surrogate authentication is the ability to authenticate on behalf of another user.
    - name: CAS GeoTracking Authentication
      content:
        - name: CAS GoogleMaps GeoTracking Authentication
          id: ${casSupportModuleArtifactIdPrefix}-geolocation-googlemaps
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-geolocation-googlemaps
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to geotrack authentication requests/events via GoogleMaps.
        - name: CAS MaxMind GeoTracking Authentication
          id: ${casSupportModuleArtifactIdPrefix}-geolocation-maxmind
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-geolocation-maxmind
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to geotrack authentication requests/events via MaxMind.
    - name: CAS Authentication Events
      content:
        - name: CAS MongoDb Authentication Events
          id: ${casSupportModuleArtifactIdPrefix}-events-mongo
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-events-mongo
          version: ${casVersion}
          bom: cas-bom
          description: Track CAS authentication events via a MongoDb instance.
        - name: CAS JDBC/JPA Authentication Events
          id: ${casSupportModuleArtifactIdPrefix}-events-jpa
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-events-jpa
          version: ${casVersion}
          bom: cas-bom
          description:  Track CAS authentication events via a JDBC instance.
        - name: CAS Memory Authentication Events
          id: ${casSupportModuleArtifactIdPrefix}-events-memory
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-events-memory
          version: ${casVersion}
          bom: cas-bom
          description: Track CAS authentication events in memory.
    - name: CAS Throttling Authentication
      content:
        - name: CAS JDBC Throttling Authentication
          id: ${casSupportModuleArtifactIdPrefix}-throttle-jdbc
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-throttle-jdbc
          version: ${casVersion}
          bom: cas-bom
          description: Throttle CAS authentication requests via a JDBC instance.
    - name: CAS Delegated Authentication
      content:
        - name: CAS Delegated Authentication
          id: ${casSupportModuleArtifactIdPrefix}-pac4j-webflow
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-pac4j-webflow
          version: ${casVersion}
          bom: cas-bom
          description: Enable delegated authentication support to external identity providers.
          keywords:
            - delegation
            - pac4j
        - name: CAS WsFederation (ADFS) Delegated Authentication
          id: ${casSupportModuleArtifactIdPrefix}-wsfederation-webflow
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-wsfederation-webflow
          version: ${casVersion}
          bom: cas-bom
          description: Enable delegated authentication support to an external ADFS instance.
    - name: CAS Authentication Methods
      content:
        - name: CAS Radius Authentication
          id: ${casSupportModuleArtifactIdPrefix}-radius
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-radius
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use RADIUS for primary authentication.
        - name: CAS LDAP Authentication
          id: ${casSupportModuleArtifactIdPrefix}-ldap
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-ldap
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use LDAP for primary authentication.
        - name: CAS JDBC Authentication
          id: ${casSupportModuleArtifactIdPrefix}-jdbc
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-jdbc
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use JDBC for primary authentication.
        - name: CAS X.509 Authentication
          id: ${casSupportModuleArtifactIdPrefix}-x509-webflow
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-x509-webflow
          version: ${casVersion}
          bom: cas-bom
          description:
        - name: CAS SPNEGO Authentication
          id: ${casSupportModuleArtifactIdPrefix}-spnego-webflow
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-spnego-webflow
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use SPNEGO for primary authentication.
        - name: CAS Remote Address Authentication
          id: ${casSupportModuleArtifactIdPrefix}-generic-remote-webflow
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-generic-remote-webflow
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use Remote-Address for primary authentication.
        - name: CAS Trusted Authentication
          id: ${casSupportModuleArtifactIdPrefix}-trusted-webflow
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-trusted-webflow
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use Trusted Remote-User for primary authentication.
        - name: CAS Token (JWT) Authentication Core
          id: ${casSupportModuleArtifactIdPrefix}-token
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-token
          version: ${casVersion}
          bom: cas-bom
          description:  Allow CAS to issue JWTs for primary authentication.
        - name: CAS Token (JWT) Authentication Webflow
          id: ${casSupportModuleArtifactIdPrefix}-token-webflow
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-token-webflow
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to accept JWTs for primary authentication.
        - name: CAS MongoDb Authentication
          id: ${casSupportModuleArtifactIdPrefix}-mongo
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-mongo
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use MongoDb for primary authentication.
        - name: CAS Apache Shiro Authentication
          id: ${casSupportModuleArtifactIdPrefix}-generic
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-generic
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use Apache Shiro for primary authentication.
        - name: CAS REST Authentication
          id: ${casSupportModuleArtifactIdPrefix}-rest-authentication
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-rest-authentication
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use REST for primary authentication.
        - name: CAS Basic Authentication
          id: ${casSupportModuleArtifactIdPrefix}-basic
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-basic
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use Digest Auth for primary authentication.
        - name: CAS Digest Authentication
          id: ${casSupportModuleArtifactIdPrefix}-digest-authentication
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-digest-authentication
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use Basic Auth for primary authentication.
        - name: CAS Blacklist Authentication
          id: ${casSupportModuleArtifactIdPrefix}-generic-blacklist
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-generic
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use blacklisted credentials for primary authentication.
        - name: CAS File-based Authentication
          id: ${casSupportModuleArtifactIdPrefix}-generic-file
          groupId: ${casModuleGroupId}
          artifactId: ${casSupportModuleArtifactIdPrefix}-generic
          version: ${casVersion}
          bom: cas-bom
          description: Allow CAS to use whitelisted credentials for primary authentication.
          keywords:
            - whitelist
    - name: CAS Web Applications
      content:
        - name: CAS Web Application
          id: cas-server-webapp
          groupId: ${casModuleGroupId}
          artifactId: cas-server-webapp
          scope: runtime
          version: ${casVersion}
          bom: cas-bom
          description: CAS Server Vanilla Web Application
          facets:
            - web
            - webapp
          starter: true
        - name: CAS Web Application Tomcat
          id: cas-server-webapp-tomcat
          groupId: ${casModuleGroupId}
          artifactId: cas-server-webapp-tomcat
          version: ${casVersion}
          scope: runtime
          bom: cas-bom
          description: CAS Server Web Application with Embedded Jetty
          facets:
            - web
            - webapp
          starter: true
        - name: CAS Web Application Jetty
          id: cas-server-webapp-jetty
          groupId: ${casModuleGroupId}
          artifactId: cas-server-webapp-jetty
          version: ${casVersion}
          scope: runtime
          bom: cas-bom
          description: CAS Server Web Application with Embedded Undertow
          facets:
            - web
            - webapp
          starter: true
        - name: CAS Web Application Undertow
          id: cas-server-webapp-undertow
          groupId: ${casModuleGroupId}
          artifactId: cas-server-webapp-undertow
          version: ${casVersion}
          scope: runtime
          bom: cas-bom
          description: CAS Server Web Application with Embedded Undertow
          facets:
            - web
            - webapp
          starter: true
  javaVersions:
    - id: 1.8
  languages:
    - name: Java
      id: java
  packagings:
    - name: WAR
      id: war
  bootVersions:
    - name: 1.5.3
      id: 1.5.3.RELEASE
      default: true
  types:
    - name: Maven Project
      id: maven-project
      description: Generate a Maven based CAS Overlay
      sts-id: starter.zip
      tags:
        build: maven
        format: project
      default: true
      action: /starter.zip
    - name: Gradle Project
      id: gradle-project
      description: Generate a Gradle based CAS Overlay
      sts-id: starter.zip
      tags:
        build: gradle
        format: project
      default: false
      action: /starter.zip
  name:
    value: "CAS WAR Overlay"
  group-id:
    value: "org.apereo.cas"
  description:
    value: >
      Apereo CAS WAR Overlay allows you to download a pre-built vanilla CAS web application
      server provided and override/insert specific behavior into it.
  package-name:
    value: "org.apereo.cas"
  artifact-id:
    value: "cas-overlay"
spring:
  thymeleaf:
    cache: false
