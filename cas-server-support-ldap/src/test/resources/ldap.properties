#========================================
# General properties
#========================================
ldap.url=ldap://localhost:389

# Start TLS for SSL connections
ldap.useStartTLS=false

# Directory root DN
ldap.rootDn=dc=springframework,dc=org

# Base DN of users to be authenticated
ldap.baseDn=ou=people,dc=springframework,dc=org

# LDAP connection timeout in milliseconds
ldap.connectTimeout=3000

# Whether to use StartTLS (probably needed if not SSL connection)
# ldap.useStartTLS=true

# SSL certificate of LDAP directory to trust
# ldap.trustedCert=file:/path/to/ldap-test-1.middleware.vt.edu.pem

# Manager credential DN
ldap.managerDn=cn=Directory Manager

# Manager credential password
ldap.managerPassword=Password

#========================================
# LDAP connection pool configuration
#========================================
ldap.pool.minSize=1
ldap.pool.maxSize=10
ldap.pool.validateOnCheckout=false
ldap.pool.validatePeriodically=true

# Amount of time in milliseconds to block on pool exhausted condition
# before giving up.
ldap.pool.blockWaitTime=3000

# Frequency of connection validation in seconds
# Only applies if validatePeriodically=true
ldap.pool.validatePeriod=300

# Attempt to prune connections every N seconds
ldap.pool.prunePeriod=300

# Maximum amount of time an idle connection is allowed to be in
# pool before it is liable to be removed/destroyed
ldap.pool.idleTime=600

#========================================
# Authentication
#========================================
ldap.authn.searchFilter=(mail={user})

# Ldap domain used to resolve dn
ldap.domain=springframework.org

#========================================
# User Details
#========================================
ldap.role.baseDn=ou=people,dc=springframework,dc=org
ldap.role.searchFilter=&(objectclass=casServiceUserDetails)(member={0})
ldap.user.searchFilter=&(objectclass=casServiceUserDetails)(mail={0})

#========================================
# Registered Service
#========================================
ldap.service.baseDn=ou=people,dc=springframework,dc=org
