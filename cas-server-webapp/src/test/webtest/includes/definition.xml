<!-- Adding the ANT taskdef -->
<property file="properties/canoo.properties" />

<taskdef file="${webtest.home}/webtestTaskdefs.properties">
	<classpath>
		<fileset dir="${webtest.home}">
			<include name="lib/**/*.jar" />
		</fileset>
		<pathelement path="${webtest.home}/lib" />
	</classpath>
</taskdef>