<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/util
       http://www.springframework.org/schema/util/spring-util.xsd">

    <alias name="${primaryAuthenticationHandler}" alias="primaryAuthenticationHandler" />
    <alias name="${primaryPrincipalResolver}" alias="primaryPrincipalResolver" />
    <alias name="${themeResolver}" alias="themeResolver" />
    <alias name="${serviceRegistryDao}" alias="serviceRegistryDao" />
    <alias name="${ticketRegistry}" alias="ticketRegistry" />
    <alias name="${grantingTicketExpirationPolicy}" alias="grantingTicketExpirationPolicy" />
    <alias name="${authenticationPolicy}" alias="authenticationPolicy" />
    <alias name="${authenticationPolicyFactory}" alias="authenticationPolicyFactory" />
    <alias name="${authenticationThrottle}" alias="authenticationThrottle" />
    <alias name="${defaultCookieCipherExecutor}" alias="defaultCookieCipherExecutor" />
    <alias name="${principalFactory}" alias="principalFactory" />
    <alias name="${authenticationTransactionManager}" alias="authenticationTransactionManager" />
    <alias name="${principalElectionStrategy}" alias="principalElectionStrategy" />
    <alias name="${auditTrailManager}" alias="auditTrailManager" />

    <util:list id="monitorsList">
        <ref bean="memoryMonitor" />
        <ref bean="sessionMonitor" />
    </util:list>

</beans>