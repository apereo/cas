/**
 * <p>This package includes the various default expiration policies
 * included with CAS. A ticket is given an expiration policy such that if
 * you ask a ticket if it is expired, it will check itself against the
 * expiration policy and then determine what response to give.</p>
 * <p>Current implementations include Never Expires, Number of Uses, and a
 * Time Out policy.</p>
 * @since 3.0
 */
package org.apereo.cas.ticket.support;

