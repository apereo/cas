apply from: project.file("../gradle/webapp.gradle")
apply from: project.file("../gradle/bootwebapp.gradle")

description = "Apereo CAS Web Application via Undertow"

dependencies {
    compile libraries.springbootundertow
}
