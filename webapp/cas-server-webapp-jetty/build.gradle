apply plugin: "org.apereo.cas.project-conventions"

ext {
    publishMetadata = true
    projectMetadata = [
            title: "Jetty",
            facets: ["web"],
            aliases: ["jetty"],
            category: "Web Application"
    ]
}
apply plugin: "org.apereo.cas.war-conventions"
description = "Builds a CAS web application packaged for Jetty-based deployments."

dependencies {
    implementation libraries.springbootjetty

    testImplementation project(path: ":core:cas-server-core-services", configuration: "tests")
    testImplementation project(path: ":core:cas-server-core", configuration: "tests")
    testImplementation project(path: ":core:cas-server-core-authentication-api", configuration: "tests")
    testImplementation project(path: ":core:cas-server-core-authentication", configuration: "tests")
    testImplementation project(path: ":core:cas-server-core-util-api", configuration: "tests")
}
